# Safari controls and Speck RMM — September 23, 2026

Chat runtime `c1d6bc5` is deployed at `https://chat.slide.recipes` and pushed to
GitHub master. It includes Safari control fix `f3beb6e` and the site-scoped Speck
RMM integration. The release preserves the previously deployed `5111bf9`
workspace improvements, which were ahead of GitHub's old default branch.

## Behavior and checks

Single-choice selects use explicit appearance, theme-aware arrows, reserved text
padding, visible focus and legible disabled text. Native picker and keyboard
behavior remain available; forced-colors mode restores browser control styling.
The mobile client selector uses 16px text. Native macOS Safari checks covered
light/dark forms, long client names, source/client/region menus, mouse selection,
keyboard selection, and the new Speck RMM form. The deployed form was inspected
again in Safari.

Speck RMM is available under Connections and exposes inventory, health, volumes,
services, active alerts and existing patch reports. See
[connection setup and limits](speck-integration.md). Speck runtime `682fc0d`
adds admin-managed, expiring and revocable tokens restricted to one exact Site.
Existing fleet Site assignments and user workspace connections were preserved.

Validation passed: 55 Chat Python tests, 26 JavaScript tests, JavaScript syntax and
Git whitespace checks. The 55 Python tests also passed in the production Python
environment against disposable state before activation. Speck passed 120 backend
tests, 28 web unit tests, 90 Chromium/WebKit browser checks, Ruff, and its web build.
Public Chat health is healthy, all four JavaScript/CSS files match the committed
SHA-256 hashes, the template references the new versions, and unauthenticated
tool access returns 401. Speck health and served asset hashes also passed.

The positive production test used a temporary, explicitly QA-only source in an
isolated Chat workspace. Public APIs verified connector creation, companion
authentication, inventory, alerts, device detail, volumes, services and patch
reports. Cross-client, wrong-Site and out-of-Site device access were rejected;
revoking the Speck token also blocked subsequent Chat reads. The test source,
workspace and companion token were removed, the Speck token revoked, and the
temporary admin session logged out. No model request or endpoint operation was
needed. This test mapping was not retained as a customer connection.

## Release and rollback

- Chat: `/opt/slide-recipes/releases/20260923T014416Z-speck/chat`.
- Chat backup: `/var/backups/slide-recipes/20260923T014416Z-speck`.
- Speck: `/opt/speck/releases/20260923T014640Z-chat-integration-682fc0d`.
- Speck backup: `/var/lib/speck-rollback/20260923T014640Z-chat-integration-682fc0d`.

Chat preflight compared all 15 baseline runtime files, staged file hashes,
dependency requirements and active workspace leases. The rollout retained a
private database/key/environment backup and the previous systemd unit, then
activated an immutable release. Production configuration hashes remained
unchanged. On `slide.recipes`, after checking for later releases and active work,
`python3 /var/backups/slide-recipes/20260923T014416Z-speck/rollback.py` restores
the previous application without replacing current workspace data or credentials.
That previous release already includes the Safari fix.

The earlier Safari-only rollback is
`/var/backups/slide-chat/safari-20260923/rollback.py`; it applies to the old release
directory, not the new integration release. Do not run it as a substitute for the
current release rollback.

Private deployment manifests, scripts and public verification results are retained
in the SlideDev hub's ignored `output/slide-chat-safari-speck/` directory.
